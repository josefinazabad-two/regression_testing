version: 2


macros:
  - name: startup
    description: | 
      Operation to run at start of DBT execution.

      * Create database objects for custom logging if not exists
      * Adds a start indicator to activity table
